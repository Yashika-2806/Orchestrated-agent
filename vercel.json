{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --force",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
